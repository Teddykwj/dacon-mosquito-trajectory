services:
  mosquito:
    build: .
    volumes:
      - ./data:/app/data
      - ./output:/app/output
      - ./logs:/app/logs
    working_dir: /app
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
